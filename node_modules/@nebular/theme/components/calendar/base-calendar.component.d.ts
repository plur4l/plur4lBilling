/**
 * @license
 * Copyright Akveo. All Rights Reserved.
 * Licensed under the MIT License. See License.txt in the project root for license information.
 */
import { EventEmitter, Type } from '@angular/core';
import { NbCalendarCell, NbCalendarSize, NbCalendarViewMode } from '../calendar-kit';
/**
 * The basis for calendar and range calendar components.
 * Encapsulates common behavior - store calendar state and perform navigation
 * between pickers.
 * */
export declare class NbBaseCalendarComponent<T> {
    /**
     * Defines if we should render previous and next months
     * in the current month view.
     * */
    boundingMonth: boolean;
    /**
     * Defines active view for calendar.
     * */
    activeViewMode: NbCalendarViewMode;
    /**
     * Minimum available date for selection.
     * */
    min: Date;
    /**
     * Maximum available date for selection.
     * */
    max: Date;
    /**
     * Predicate that decides which cells will be disabled.
     * */
    filter: (Date) => boolean;
    /**
     * Custom day cell component. Have to implement `NbCalendarCell` interface.
     * */
    dayCellComponent: Type<NbCalendarCell<T>>;
    /**
     * Custom month cell component. Have to implement `NbCalendarCell` interface.
     * */
    monthCellComponent: Type<NbCalendarCell<T>>;
    /**
     * Custom year cell component. Have to implement `NbCalendarCell` interface.
     * */
    yearCellComponent: Type<NbCalendarCell<T>>;
    /**
     * Size of the calendar and entire components.
     * Can be 'medium' which is default or 'large'.
     * */
    size: NbCalendarSize;
    /**
     * Value which will be rendered as selected.
     * */
    date: T;
    /**
     * Emits date when selected.
     * */
    dateChange: EventEmitter<T>;
    readonly medium: boolean;
    readonly large: boolean;
    ViewMode: typeof NbCalendarViewMode;
    visibleDate: Date;
    setViewMode(viewMode: NbCalendarViewMode): void;
    setVisibleDate(visibleDate: Date): void;
    prevMonth(): void;
    nextMonth(): void;
    prevYears(): void;
    nextYears(): void;
    navigateToday(): void;
    private changeVisibleMonth(direction);
    private changeVisibleYear(direction);
}
