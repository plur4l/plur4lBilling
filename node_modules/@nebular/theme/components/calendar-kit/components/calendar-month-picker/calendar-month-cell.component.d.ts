/**
 * @license
 * Copyright Akveo. All Rights Reserved.
 * Licensed under the MIT License. See License.txt in the project root for license information.
 */
import { EventEmitter } from '@angular/core';
import { NbLocaleService } from '../../services';
import { NbCalendarCell } from '../../model';
export declare class NbCalendarMonthCellComponent implements NbCalendarCell<Date> {
    private localeService;
    date: Date;
    selectedValue: Date;
    min: Date;
    max: Date;
    select: EventEmitter<Date>;
    constructor(localeService: NbLocaleService);
    readonly selected: boolean;
    readonly today: boolean;
    readonly disabled: boolean;
    readonly month: string;
    onClick(): void;
    private smallerThanMin();
    private greaterThanMax();
    private monthStart();
    private monthEnd();
}
