/**
 * @license
 * Copyright Akveo. All Rights Reserved.
 * Licensed under the MIT License. See License.txt in the project root for license information.
 */
import { ComponentFactoryResolver, EventEmitter, OnChanges, Type, ViewContainerRef } from '@angular/core';
import { NbCalendarCell } from '../../model';
export declare class NbCalendarPickerRowComponent<T> implements OnChanges {
    private cfr;
    row: Date[];
    selectedValue: T;
    visibleDate: Date;
    component: Type<NbCalendarCell<T>>;
    min: Date;
    max: Date;
    filter: (Date) => boolean;
    select: EventEmitter<Date>;
    containerRef: ViewContainerRef;
    constructor(cfr: ComponentFactoryResolver);
    ngOnChanges(): void;
    private patchWithContext(component, date);
}
