export declare namespace NbDateTimeUtil {
    const DAYS_IN_WEEK: number;
    const markIfHoliday: (name: any, i: any) => {
        name: any;
        isHoliday: boolean;
    };
    const createDate: (year: number, month: number, date: number) => Date;
    const isSameYear: (date1: Date, date2: Date) => boolean;
    const isSameYearSafe: (date1: Date, date2: Date) => boolean;
    const isSameMonth: (date1: Date, date2: Date) => boolean;
    const isSameMonthSafe: (date1: Date, date2: Date) => boolean;
    const isSameDay: (date1: Date, date2: Date) => boolean;
    const isSameDaySafe: (date1: Date, date2: Date) => boolean;
    const compareDates: (date1: Date, date2: Date) => number;
    const isBetween: (date: Date, start: Date, end: Date) => boolean;
    const clone: (date: Date) => Date;
    const getMonthStart: (date: Date) => Date;
    const getMonthEnd: (date: Date) => Date;
    const getYearStart: (date: Date) => Date;
    const getYearEnd: (date: Date) => Date;
    const getNumberOfDaysInMonth: (date: Date) => number;
    const addDay: (date: Date, num: number) => Date;
    const addMonth: (date: Date, num: number) => Date;
    const addYear: (date: Date, num: number) => Date;
    const createDateRangeForMonth: (date: Date) => Date[];
}
